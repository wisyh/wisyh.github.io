# Supabase Todo App with GitHub Pages

一个基于 Supabase + Vite + GitHub Pages 的动态 Todo 应用，支持实时数据同步和自动部署。

![Supabase](https://img.shields.io/badge/Supabase-3ECF8E?style=flat-square&logo=supabase&logoColor=white)
![Vite](https://img.shields.io/badge/Vite-B73BFE?style=flat-square&logo=vite&logoColor=FFD62E)
![GitHub Pages](https://img.shields.io/badge/GitHub%20Pages-222222?style=flat-square&logo=github%20pages&logoColor=white)

## 在线预览
- 🔗 **在线演示**: [https://wisyh.github.io](https://wisyh.github.io)

## 功能特性

- ✅ **实时数据同步** - 使用 Supabase 实时功能
- ✅ **任务管理** - 添加、标记状态、删除任务
- ✅ **状态标识** - 不同颜色区分任务状态
- ✅ **自动部署** - 提交即部署到 GitHub Pages
- ✅ **安全策略** - 完整的 RLS 权限控制

## 快速开始

### 1. 克隆项目
```bash
git clone https://github.com/wisyh/wisyh.github.io.git
cd wisyh.github.io
```

2. 安装依赖

```bash
npm install
```

3. 配置环境变量

创建 .env 文件：

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_KEY=your-anon-key
```

4. 启动开发服务器

```bash
npm run dev
```

打开 http://localhost:5173

数据库配置

在 Supabase SQL Editor 中执行：

```sql
-- 创建 todos 表
CREATE TABLE todos (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  task text NOT NULL,
  status text DEFAULT 'Not Started',
  inserted_at timestamp with time zone DEFAULT timezone('utc'::text, now())
);

-- 启用行级安全策略
ALTER TABLE todos ENABLE ROW LEVEL SECURITY;

-- 创建访问策略
CREATE POLICY "Allow anonymous access" 
ON todos FOR ALL 
TO anon 
USING (true) 
WITH CHECK (true);
```

项目结构

```
wisyh.github.io/
├── .github/workflows/
│   └── deploy.yml          # GitHub Actions 部署配置
├── src/
│   ├── supabaseClient.js   # Supabase 客户端配置
│   └── main.js             # 应用主逻辑
├── index.html              # 主页面
├── package.json            # 项目依赖
└── vite.config.js          # Vite 配置
```

自动部署

提交到 main 分支时自动触发部署：

```bash
git add .
git commit -m "更新功能"
git push origin main
```

GitHub Actions 会自动构建并部署到 GitHub Pages。

安全说明

· 使用 Row Level Security (RLS) 保护数据
· 仅使用公开的客户端 API Key
· 敏感密钥存储在 GitHub Secrets 中

许可证

MIT License © wisyh